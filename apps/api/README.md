# @sport-store/api

Express stub for the future Postgres-backed API.

```bash
npm run dev:api
```

Health check: `GET http://localhost:3001/api/health`

Copy `.env.example` to `.env` before connecting a database. Route stubs under `/api/*` return `501` until implemented.
